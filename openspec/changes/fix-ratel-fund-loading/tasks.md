## 1. 诊断与验证

- [x] 1.1 在 `api.ts` 中添加启动诊断日志，输出 BASE_URL 和环境变量来源
- [x] 1.2 验证 `next.config.ts` 中的 `DefinePlugin` 正确注入 `VITE_API_BASE_URL`
- [ ] 1.3 在浏览器 Network Tab 中验证请求 URL 格式正确（`http://localhost:8003/api/v1/...`）
- [ ] 1.4 验证 token 从 localStorage 正确读取并附加到请求头

## 2. 数据缓存机制

- [x] 2.1 创建 `hooks/useDataCache.ts`，实现带 TTL 的缓存 hook
- [x] 2.2 定义缓存配置常量（列表 5 分钟、业绩 10 分钟、分析 30 分钟）
- [x] 2.3 实现缓存失效和手动清除功能
- [x] 2.4 添加缓存命中/未命中的调试日志

## 3. 分层数据加载

- [x] 3.1 修改 `Fund/index.tsx`，首屏只调用 `getFundList`
- [x] 3.2 将业绩对比数据加载移至基金选中事件处理
- [x] 3.3 将因子分析数据加载移至详情面板展开事件处理
- [x] 3.4 为各数据区块添加独立的加载状态管理

## 4. 刷新节流机制

- [x] 4.1 创建 `hooks/useThrottledRefresh.ts`，实现节流刷新 hook
- [x] 4.2 创建 `components/RefreshButton.tsx`，包含倒计时和禁用状态
- [x] 4.3 实现轻量级数据刷新配置（最小间隔 5 秒，冷却 3 秒）
- [x] 4.4 实现重量级数据刷新配置（最小间隔 30 秒，冷却 10 秒）
- [x] 4.5 添加 Shift + 点击强制刷新功能

## 5. 数据新鲜度指示

- [x] 5.1 添加"最后更新时间"显示，使用相对时间格式
- [x] 5.2 实现陈旧数据视觉提示（超过 50% TTL）
- [x] 5.3 添加悬停显示精确更新时间的 tooltip

## 6. 错误处理增强

- [x] 6.1 在 `request.ts` 中增强网络错误诊断信息
- [x] 6.2 添加 CORS 错误检测和日志
- [x] 6.3 增强 401 错误日志，显示 token 状态
- [ ] 6.4 为各数据区块添加独立的错误状态和重试按钮

## 7. 集成与测试

- [x] 7.1 在 Fund 页面集成 RefreshButton 组件
- [ ] 7.2 验证分层加载在正常网络条件下工作
- [ ] 7.3 验证缓存机制在页面导航后生效
- [ ] 7.4 验证刷新节流在快速点击时生效
- [ ] 7.5 测试后端服务不可用时的错误处理
