# 基金分析报告系统 PRD

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 可拖拽基金分析报告系统 PRD |
| 文档版本 | V1.0 |
| 文档作者 | 产品团队 |
| 创建日期 | 2026-01-19 |
| 最后更新 | 2026-01-19 |
| 文档状态 | 评审中 |
| 项目优先级 | P0 |

### 更新记录

| 版本 | 日期 | 作者 | 更新内容 |
|------|------|------|----------|
| V1.0 | 2026-01-19 | 产品团队 | 初稿，完成核心功能需求定义 |

---

## 1. 需求概述

### 1.1 需求背景

**业务背景**

投资经理和基金分析师在日常工作中需要对基金产品进行多维度分析，包括收益统计、风险评估、业绩归因等。目前存在以下痛点：

- 分析工具固化，无法根据个人习惯和分析需求灵活配置
- 生成报告需要手动整合多个数据源，效率低下
- 不同场景（周报、月报、专项分析）需要不同的展示模块，切换繁琐
- 输出报告缺乏统一的品牌样式，专业度参差不齐

**用户场景**

投资经理小王每周需要为持仓基金出具分析周报：

1. 打开分析工具，选择目标基金
2. 从几十个分析指标中筛选出本次需要的内容
3. 调整各模块的排列顺序和显示参数
4. 导出为统一格式的PDF报告
5. 下周复用相同的报告模板，仅更新数据

**需求来源**

- 用户调研：投资经理反馈现有工具灵活性不足
- 竞品分析：主流基金分析平台均已支持自定义报告
- 业务需求：提升专业用户工作效率和产品粘性

### 1.2 产品目标

**用户目标**

- 灵活选择分析模块，按需组装个性化分析视图
- 通过拖拽方式快速调整报告布局
- 保存常用配置为模板，提升重复性工作效率
- 一键导出专业格式的PDF报告

**商业目标**

- 提升专业用户（投资经理、分析师）的使用频次和停留时长
- 增强产品差异化竞争力
- 为高级功能付费转化奠定基础

**目标用户**

| 用户类型 | 描述 | 占比 |
|---------|------|------|
| 投资经理 | 负责基金投资决策，需要深度分析基金表现 | 60% |
| 基金分析师 | 为投资经理提供研究支持，产出分析报告 | 30% |
| 产品经理 | 基金公司产品部门，跟踪产品运营数据 | 10% |

### 1.3 产品范围

**本期范围（MVP）**

| 功能模块 | 说明 | 优先级 |
|---------|------|--------|
| 模块添加 | 点击左侧导航添加分析模块到画布 | P0 |
| 拖拽排序 | 拖拽调整模块位置顺序 | P0 |
| 模块大小调整 | 拖拽模块边缘调整显示尺寸 | P0 |
| 模块删除 | 从画布移除模块 | P0 |
| 模块克隆 | 复制模块用于对比分析 | P0 |
| 全局参数配置 | 设置基金、日期范围、基准等全局参数 | P0 |
| 模块参数配置 | 单独配置模块参数，支持覆盖全局设置 | P0 |
| 全局样式设置 | 设置主题颜色、字体、背景等视觉样式 | P0 |
| 报告保存 | 保存当前报告内容和配置 | P0 |
| PDF导出 | 导出为PDF格式报告 | P0 |
| 布局模板 | 保存/加载报告布局模板 | P1 |
| 样式模板 | 保存/加载视觉样式模板 | P1 |

**不在范围内**

| 功能 | 说明 | 后续规划 |
|------|------|---------|
| 点评功能 | 为模块添加文字注释 | V2.0 |
| 模板分享 | 团队共享报告模板 | V2.0 |
| 撤销/重做 | 操作历史管理 | 暂不规划 |
| 模块联动 | 跨模块数据高亮联动 | V2.0 |

**依赖条件**

- 底层数据服务：各分析模块所需的基金数据API
- 用户系统：用户登录态和权限验证
- 文件服务：PDF生成和存储服务

---

## 2. 功能架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        基金分析报告系统                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌───────────┐    ┌────────────────────────┐    ┌──────────────────┐  │
│   │           │    │                        │    │                  │  │
│   │  模块导航  │    │       画布区域          │    │   配置面板       │  │
│   │  (左侧)   │    │    (中间主区域)         │    │   (右侧)        │  │
│   │           │    │                        │    │                  │  │
│   │ • 基本信息 │    │  ┌──────┐  ┌──────┐   │    │ 全局设置        │  │
│   │ • 基础分析 │    │  │ 模块1 │  │ 模块2 │   │    │ ├─ 全局参数    │  │
│   │   ├ 收益  │    │  └──────┘  └──────┘   │    │ └─ 全局样式    │  │
│   │   ├ 风险  │    │  ┌──────────────────┐  │    │                  │  │
│   │   ├ 业绩  │    │  │     模块3        │  │    │ 模块设置        │  │
│   │   └ 归因  │    │  └──────────────────┘  │    │ ├─ 数据参数    │  │
│   │ • 股票策略 │    │                        │    │ └─ 显示配置    │  │
│   │           │    │                        │    │                  │  │
│   └───────────┘    └────────────────────────┘    └──────────────────┘  │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │  顶部栏：报告名称 | 全局筛选条件 | 导出 | 保存                      │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 模块层级结构

```
一级板块
├── 产品信息
│   └── 产品表头
│   └── 基本信息
│   └── ...
├── 基础分析
│   ├── 收益统计（二级）
│   │   ├── 历史盈利概率（三级-叶子节点）
│   │   ├── 滚动夏普
│   │   ├── 年度收益
│   │   ├── 月度收益
│   │   ├── 季度收益
│   │   ├── 滚动收益
│   │   ├── 区间收益
│   │   ├── 滚动收益率分布
│   │   └── 净值走势-收益走势控件组
│   ├── 风险统计（二级）
│   │   ├── 滚动Beta
│   │   ├── VaR分布
│   │   ├── 滚动波动率
│   │   ├── 动态回撤
│   │   ├── 滚动相关系数
│   │   ├── 最大回撤
│   │   └── 动态回撤表
│   ├── 业绩表现
│   ├── 组合分析
│   └── 净值归因
└── 股票策略
    └── ...
```

**说明**：用户只能添加三级叶子节点（如"滚动夏普"）到画布，不能添加整个二级分类。

---

## 3. 功能需求详细说明

### 3.1 页面布局

#### 3.1.1 顶部栏

**布局结构**

```
┌────────────────────────────────────────────────────────────────────────┐
│ ☰目录 │ 请输入报告名称    │ 上次保存于XX │     [导出]  [✓ 保存]       │
├────────────────────────────────────────────────────────────────────────┤
│ 🔍搜索基金 │ 成立以来(成立日-至今) │ 中证500 │ 周度 │ 复权净值         │
└────────────────────────────────────────────────────────────────────────┘
```

**功能说明**

| 元素 | 功能 | 交互说明 |
|------|------|---------|
| 目录按钮 | 展开/收起左侧导航 | 点击切换导航显示状态 |
| 报告名称 | 输入/编辑报告标题 | 输入框，支持实时编辑 |
| 保存状态 | 显示上次保存时间 | 只读文本，自动更新 |
| 导出按钮 | 打开导出弹窗 | 点击弹出导出配置 |
| 保存按钮 | 保存当前报告 | 点击保存，按钮变为已保存状态 |
| 全局筛选条件 | 设置影响所有模块的筛选条件 | 下拉选择 |

**全局筛选条件**

| 筛选项 | 说明 | 选项示例 |
|--------|------|---------|
| 基金 | 选择分析的目标基金 | 搜索下拉选择 |
| 日期范围 | 分析的时间区间 | 成立以来、近1年、近3年、自定义 |
| 基准 | 业绩比较基准 | 中证500、沪深300、自定义 |
| 频率 | 数据统计频率 | 日度、周度、月度 |
| 净值类型 | 净值计算方式 | 复权净值、单位净值 |

#### 3.1.2 左侧导航

**布局说明**

- 树形结构展示所有可添加的分析模块
- 支持展开/折叠二级分类
- 三级叶子节点可点击添加到画布
- 已添加的模块在导航中高亮显示

**交互规则**

| 操作 | 结果 |
|------|------|
| 点击一级/二级节点 | 展开/折叠子节点 |
| 点击三级叶子节点 | 将该模块添加到画布末尾 |
| 点击已添加的叶子节点 | 画布滚动到该模块位置并选中 |

#### 3.1.3 中间画布

**布局说明**

- 模块卡片垂直排列
- 支持拖拽调整顺序
- 支持拖拽调整卡片大小
- 选中卡片时右侧显示配置面板

**卡片结构**

```
┌─────────────────────────────────────────────────────────────┐
│ 区间收益 ⓘ                              [算术超额] [···]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                    📊 图表/表格内容                          │
│                                                             │
│                                                          ↘️ │
└─────────────────────────────────────────────────────────────┘
```

**卡片元素说明**

| 元素 | 功能 |
|------|------|
| 模块标题 | 显示模块名称 |
| ⓘ 说明图标 | 悬浮显示模块说明（可通过配置关闭） |
| 快捷切换按钮 | 部分模块支持快速切换显示模式（如算术超额/几何超额） |
| ··· 更多菜单 | 克隆、删除操作 |
| 内容区 | 展示图表或表格 |
| 右下角拖拽点 | 拖拽调整卡片大小 |

**更多菜单选项**

| 选项 | 功能 | 说明 |
|------|------|------|
| 克隆 | 复制当前模块 | 在当前模块下方创建副本，参数独立 |
| 删除 | 移除模块 | 从画布删除，导航中取消高亮 |

#### 3.1.4 右侧配置面板

**面板状态**

| 状态 | 显示内容 |
|------|---------|
| 未选中模块 | 显示"全局设置"（全局参数 + 全局样式） |
| 选中模块 | 显示该模块的参数配置 |

**全局设置 - 全局参数 Tab**

| 配置项 | 说明 |
|--------|------|
| 基金 | 同顶部全局筛选 |
| 日期范围 | 同顶部全局筛选 |
| 基准 | 同顶部全局筛选 |
| 频率 | 同顶部全局筛选 |
| 净值类型 | 同顶部全局筛选 |
| 报告类型 | 产品/经理/组合 |
| 报告类别 | 周报/月报/季报/专项 |
| 导航设置 | 导出PDF时的目录配置 |
| 批量控件参数设置 | 批量修改所有模块的某个参数 |

**导航设置选项**

| 选项 | 说明 |
|------|------|
| 打开内容导航 | 导出PDF是否包含目录 |
| 内容展示一级导航名字 | 目录是否显示一级标题（如"基础分析"） |
| 导航中显示底层控件名字 | 目录是否显示具体模块名（如"滚动夏普"） |

**全局设置 - 全局样式 Tab**

| 配置项 | 说明 |
|--------|------|
| 背景 | 报告背景色 |
| 内容 | 内容区背景色 |
| 主题颜色 | 预设配色方案选择 |
| 文字-标题 | 字号、字体、颜色、修饰（加粗/下划线/斜体） |
| 文字-图例 | 字号、字体、颜色、修饰 |
| X/Y轴 | 字号、字体、颜色、修饰 |
| 导出模板样式 | 保存当前样式为模板 / 加载已有样式模板 |

**模块参数配置**

不同模块类型显示不同的配置项，以"区间收益"模块为例：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| 净值频率 | 数据频率 | 跟随全局 |
| 基金 | 分析标的 | 跟随全局 |
| 基准 | 比较基准 | 跟随全局 |
| 对比基金 | 用于对比的其他基金 | 空 |
| 附加指标 | 额外展示的指标 | 所有 |
| 日期范围 | 时间区间 | 跟随全局 |
| 净值类型 | 净值计算方式 | 跟随全局 |
| 添加超额 | 是否显示超额收益 | 算术超额 |

**参数锁定机制**

- 参数右侧有 🔒 锁定图标
- 点击锁定：该参数不再跟随全局设置变化
- 解锁后：恢复跟随全局设置

**功能配置区**

针对表格类模块的额外配置：

| 配置项 | 说明 |
|--------|------|
| 按钮 | 配置可用的操作按钮 |
| 格式 | 数据格式化设置 |
| 提示-说明文字 | 是否显示ⓘ说明图标 |
| 首列固定 | 表格首列是否冻结 |
| 行列转置 | 表格行列互换 |
| 跳转 | 配置点击跳转目标 |

---

### 3.2 核心功能

#### 3.2.1 模块添加

**功能描述**

用户通过点击左侧导航的三级叶子节点，将分析模块添加到画布。

**使用场景**

用户想分析基金的风险指标，点击导航中的"滚动Beta"，该模块卡片出现在画布末尾。

**操作流程**

1. 用户浏览左侧导航树
2. 展开目标分类（如"风险统计"）
3. 点击叶子节点（如"滚动Beta"）
4. 系统在画布末尾添加该模块卡片
5. 导航中该节点显示为"已添加"状态
6. 模块自动获取数据并展示

**规则说明**

| 规则 | 说明 |
|------|------|
| 添加位置 | 始终添加到画布末尾 |
| 默认参数 | 新添加的模块默认跟随全局参数 |
| 重复添加 | 允许添加同一模块多次（通过克隆实现更佳） |
| 数量上限 | 无上限，最多为所有叶子节点之和 |

**验收标准**

- [ ] 点击三级叶子节点后，模块卡片出现在画布末尾
- [ ] 新模块自动加载数据并展示
- [ ] 导航中已添加的节点有明显的视觉区分
- [ ] 点击已添加的节点，画布滚动到该模块位置

#### 3.2.2 拖拽排序

**功能描述**

用户通过拖拽卡片调整模块在画布中的排列顺序。

**使用场景**

用户想把"滚动Beta"模块移动到"区间收益"模块上方，拖拽"滚动Beta"卡片并放置到目标位置。

**操作流程**

1. 用户鼠标悬浮在模块卡片上
2. 按住卡片标题栏区域开始拖拽
3. 拖拽过程中显示放置位置指示线
4. 松开鼠标完成排序
5. 左侧导航顺序同步更新

**规则说明**

| 规则 | 说明 |
|------|------|
| 拖拽触发区域 | 卡片标题栏区域（或显示"拖拽移动位置"提示） |
| 位置指示 | 拖拽时显示蓝色水平线指示放置位置 |
| 导航同步 | 画布排序变化时，左侧导航顺序同步更新 |

**验收标准**

- [ ] 拖拽卡片时有明显的视觉反馈
- [ ] 放置位置有清晰的指示线
- [ ] 松开鼠标后卡片移动到目标位置
- [ ] 左侧导航顺序与画布保持一致

#### 3.2.3 调整大小

**功能描述**

用户通过拖拽卡片右下角调整模块的显示尺寸。

**使用场景**

用户想让某个图表模块显示更大的图表区域，拖拽右下角放大卡片。

**操作流程**

1. 鼠标悬浮在卡片右下角，光标变为调整大小图标
2. 按住并拖拽调整尺寸
3. 松开鼠标确认新尺寸
4. 模块内容自适应新尺寸重新渲染

**规则说明**

| 规则 | 说明 |
|------|------|
| 最小尺寸 | 宽度不小于画布宽度的50%，高度不小于200px |
| 最大尺寸 | 宽度不超过画布宽度，高度无上限 |
| 内容适配 | 图表自动缩放，表格列宽自适应 |

**验收标准**

- [ ] 拖拽右下角可调整卡片大小
- [ ] 调整时有实时预览
- [ ] 内容随尺寸变化自适应
- [ ] 尺寸有合理的最小/最大限制

#### 3.2.4 模块克隆

**功能描述**

复制当前模块，用于参数对比分析。

**使用场景**

用户想对比同一基金在不同时间段的区间收益，克隆"区间收益"模块，然后修改副本的日期范围参数。

**操作流程**

1. 点击模块卡片右上角的"···"菜单
2. 选择"克隆"
3. 系统在当前模块下方创建副本
4. 副本参数独立，可单独修改

**规则说明**

| 规则 | 说明 |
|------|------|
| 克隆位置 | 紧跟在原模块下方 |
| 参数继承 | 克隆时复制原模块的所有参数配置 |
| 参数独立 | 克隆后两个模块参数完全独立 |
| 命名规则 | 克隆模块名称加" (副本)"后缀 |

**验收标准**

- [ ] 点击克隆后在原模块下方出现副本
- [ ] 副本继承原模块的所有配置
- [ ] 修改副本参数不影响原模块
- [ ] 副本名称有明显区分

#### 3.2.5 模块删除

**功能描述**

从画布移除模块。

**操作流程**

1. 点击模块卡片右上角的"···"菜单
2. 选择"删除"
3. 模块从画布移除
4. 左侧导航中该节点恢复为"未添加"状态

**验收标准**

- [ ] 点击删除后模块立即从画布消失
- [ ] 导航中对应节点取消高亮状态
- [ ] 删除操作无需二次确认（简化操作流程）

#### 3.2.6 参数配置

**功能描述**

配置模块的数据参数和显示设置。

**全局参数与模块参数的关系**

```
┌─────────────────────────────────────────┐
│           全局参数（顶部/右侧面板）        │
│  基金、日期范围、基准、频率、净值类型      │
└───────────────────┬─────────────────────┘
                    │ 默认继承
                    ▼
┌─────────────────────────────────────────┐
│           模块参数（右侧面板）             │
│  默认：跟随全局                           │
│  可选：锁定为独立值（覆盖全局）            │
└─────────────────────────────────────────┘
```

**操作流程**

1. 点击画布中的模块卡片，右侧显示该模块的配置面板
2. 修改参数值
3. 如需固定参数不随全局变化，点击🔒锁定
4. 模块自动刷新数据

**验收标准**

- [ ] 选中模块后右侧显示对应的配置项
- [ ] 修改参数后模块数据立即刷新
- [ ] 锁定的参数不随全局参数变化
- [ ] 不同模块类型显示不同的配置项

#### 3.2.7 全局样式设置

**功能描述**

设置报告的整体视觉样式，包括颜色主题、字体等。

**配置项详细说明**

**主题颜色**

- 提供多套预设配色方案
- 颜色方案包含：主色、辅助色、背景色、文字色等
- 选择主题后，所有模块的图表颜色统一更新

**字体设置**

| 配置对象 | 可配置项 |
|---------|---------|
| 标题 | 字号(默认16px)、字体(默认微软雅黑)、颜色、加粗/下划线/斜体 |
| 图例 | 字号(默认12px)、字体、颜色、加粗/斜体 |
| X/Y轴 | 字号(默认12px)、字体、颜色、加粗/斜体 |

**导出模板样式**

- 可将当前样式配置保存为模板
- 可加载已保存的样式模板
- 点击"+ 制作样式"创建新样式模板

**验收标准**

- [ ] 选择主题颜色后，所有模块图表颜色统一更新
- [ ] 字体设置实时预览生效
- [ ] 可保存和加载样式模板

#### 3.2.8 报告保存

**功能描述**

保存当前报告的所有内容和配置。

**保存内容**

| 内容 | 说明 |
|------|------|
| 报告名称 | 用户输入的报告标题 |
| 全局参数 | 基金、日期范围、基准等 |
| 模块列表 | 已添加的所有模块 |
| 模块排序 | 模块的排列顺序 |
| 模块尺寸 | 每个模块的大小设置 |
| 模块参数 | 每个模块的独立参数配置 |
| 全局样式 | 颜色主题、字体设置等 |

**操作流程**

1. 点击顶部"保存"按钮
2. 系统保存所有配置
3. 按钮显示"已保存"状态
4. 显示"上次保存于 XX:XX"

**自动保存规则**

- 建议实现自动保存机制，每隔30秒或用户操作后自动保存
- 自动保存失败时给予提示

**验收标准**

- [ ] 点击保存后数据持久化
- [ ] 刷新页面后报告内容恢复
- [ ] 显示准确的保存时间

#### 3.2.9 PDF导出

**功能描述**

将当前报告导出为PDF文件。

**导出配置项**

| 配置项 | 选项 | 说明 |
|--------|------|------|
| 报告名称 | 文本输入 | 导出文件名 |
| 导出模板 | 下拉选择 | 系统默认模板 / 自定义模板 |
| 导出类型 | PDF | 当前仅支持PDF |
| 导出尺寸 | A4纵向 / A4横向 / 无纸张大小限制 | 页面尺寸 |

**操作流程**

1. 点击顶部"导出"按钮
2. 弹出导出配置弹窗
3. 设置报告名称、模板、尺寸
4. 点击"导出"按钮
5. 系统生成PDF并下载

**导出内容**

- 包含目录导航（根据导航设置配置）
- 按画布中的顺序排列各模块
- 应用全局样式设置

**验收标准**

- [ ] 导出的PDF包含所有已添加的模块
- [ ] 模块顺序与画布一致
- [ ] 样式设置正确应用
- [ ] 导航目录按配置生成
- [ ] 支持三种尺寸选项

---

## 4. 非功能需求

### 4.1 性能要求

| 指标 | 要求 |
|------|------|
| 页面加载时间 | ≤ 3秒 |
| 模块数据加载 | ≤ 2秒/模块 |
| 拖拽操作响应 | ≤ 100ms |
| PDF导出时间 | ≤ 30秒（20个模块） |
| 画布支持模块数 | ≥ 50个模块流畅运行 |

### 4.2 兼容性要求

| 平台 | 要求 |
|------|------|
| 浏览器 | Chrome 90+、Edge 90+、Safari 14+、Firefox 88+ |
| 屏幕分辨率 | 最小支持1280×720，建议1920×1080 |

### 4.3 安全性要求

- 报告数据仅限登录用户访问
- 导出的PDF不包含敏感水印（或根据需求添加）
- 基金数据访问需验证用户权限

---

## 5. 数据埋点

### 5.1 页面埋点

| 事件名称 | 触发时机 | 参数 |
|---------|---------|------|
| page_view_report | 进入报告页面 | report_id, user_id |
| report_save | 保存报告 | report_id, module_count |
| report_export | 导出报告 | report_id, export_format, export_size |

### 5.2 模块操作埋点

| 事件名称 | 触发时机 | 参数 |
|---------|---------|------|
| module_add | 添加模块 | module_type, position |
| module_delete | 删除模块 | module_type |
| module_clone | 克隆模块 | module_type |
| module_drag | 拖拽排序 | module_type, from_position, to_position |
| module_resize | 调整大小 | module_type, width, height |
| module_config_change | 修改模块参数 | module_type, param_name, param_value |

### 5.3 核心指标

| 指标 | 定义 | 目标 |
|------|------|------|
| 报告创建数 | 每日新建报告数量 | - |
| 平均模块数 | 每个报告平均添加的模块数量 | ≥ 8 |
| 导出率 | 导出报告数 / 创建报告数 | ≥ 60% |
| 模板复用率 | 从模板创建的报告数 / 总报告数 | ≥ 40% |

---

## 6. 项目规划

### 6.1 里程碑计划

| 里程碑 | 交付物 | 预计时间 |
|--------|--------|---------|
| 需求评审 | PRD文档定稿 | W1 |
| UI设计 | 设计稿交付 | W2-W3 |
| 技术评审 | 技术方案确定 | W3 |
| 开发完成 | 功能开发完成 | W4-W8 |
| 测试完成 | 测试通过 | W9-W10 |
| 上线发布 | 正式上线 | W11 |

### 6.2 风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|---------|
| 拖拽性能问题 | 中 | 高 | 预研拖拽库，做好性能测试 |
| PDF导出样式还原 | 中 | 中 | 提前调研PDF生成方案 |
| 模块数据接口延迟 | 低 | 高 | 制定接口规范，提前联调 |

---

## 7. 附录

### 7.1 名词解释

| 术语 | 解释 |
|------|------|
| 模块 | 可添加到画布的分析组件，如"滚动夏普"、"区间收益"等 |
| 画布 | 页面中间区域，用于放置和排列模块卡片 |
| 全局参数 | 影响所有模块的筛选条件，如基金、日期范围 |
| 模块参数 | 单个模块独立的配置，可覆盖全局参数 |
| 布局模板 | 保存模块组合和位置的模板 |
| 样式模板 | 保存颜色、字体等视觉配置的模板 |

### 7.2 模块清单

**基础分析 - 收益统计**

| 模块名称 | 类型 | 说明 |
|---------|------|------|
| 历史盈利概率 | 图表 | 历史盈利的概率分布 |
| 滚动夏普 | 图表 | 滚动计算的夏普比率 |
| 年度收益 | 表格 | 按年度统计的收益率 |
| 月度收益 | 表格 | 按月度统计的收益率 |
| 季度收益 | 表格 | 按季度统计的收益率 |
| 滚动收益 | 图表 | 滚动收益率曲线 |
| 区间收益 | 表格 | 指定区间的收益统计 |
| 滚动收益率分布 | 图表 | 收益率的分布直方图 |
| 净值走势-收益走势控件组 | 组合 | 净值和收益的组合图表 |

**基础分析 - 风险统计**

| 模块名称 | 类型 | 说明 |
|---------|------|------|
| 滚动Beta | 图表 | 滚动计算的Beta系数 |
| VaR分布 | 图表 | 风险价值分布 |
| 滚动波动率 | 图表 | 滚动计算的波动率 |
| 动态回撤 | 图表 | 动态回撤曲线 |
| 滚动相关系数 | 图表 | 与基准的滚动相关性 |
| 最大回撤 | 数值 | 历史最大回撤值 |
| 动态回撤表 | 表格 | 回撤明细表 |

---

## 文档结束

本PRD文档描述了可拖拽基金分析报告系统的核心功能需求。后续根据评审反馈持续更新完善。